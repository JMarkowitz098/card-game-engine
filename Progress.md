# Progress

Dated log of what's been done. `Architecture.md` holds current state only — this is the history.

## 2026-08-16

- Built `CombatResolver` (pure damage math), `PlayerState` (health/energy/charge/hand/draw/discard), and `GameState` (`DeclareAttack`/`DeclareDefense` orchestration, win detection, decline-to-defend) — full engine test suite, 24 tests.
- Built `CardGame.Cli` console harness: turn loop, event narration, color coding, pacing delays.
- Set up `.editorconfig` + analyzers, `.gitignore`, VS Code format-on-save, `CA1707` scoped-suppressed for test naming.

## 2026-08-17

- Implemented deck shuffling — `PlayerState` shuffles (Fisher-Yates) on construction, seedable via an optional `Random` param for tests. 25th test added to prove it.
- Added `StarterDeck` — tuple-list card templates expanded into a 40-card deck via `SelectMany`/`Enumerable.Repeat`. Mechanism built; card content still being authored.
- Added CSharpier (local dotnet tool + VS Code extension) for line-wrapping/layout formatting, alongside the existing analyzers.
- Added `README.md` Setup section — prerequisites + commands for a fresh clone.
- Fixed: `ProcessAttack`/`ProcessDefense` weren't checking `DeclareAttack`/`DeclareDefense`'s `Success` — an unaffordable/invalid card silently did nothing instead of being rejected. Both now retry-loop until a valid, accepted choice is made.
- Implemented "pass instead of attacking" (`DeclareAttackIntent.Card` nullable, mirrors defense) — simpler than the "draw instead" idea floated earlier; that was dropped in favor of this.
- Found and fixed a real, pre-existing bug in `ReadyNewTurn`: it drew a card for the wrong player (`opponent` instead of the actual next active player) — masked until now by test decks always running out and by the CLI's own redundant draw call. Renamed its parameters (`defender`/`defendingPlayerId` → `nextActivePlayer`/`nextActivePlayerId`) since the old names actively caused the confusion. Added a regression test.
- Found and fixed a genuine double-draw bug: `Loop()` had its own `DrawCard()` call duplicating `ReadyNewTurn`'s. Fixed by moving the match's very first draw into `GameState`'s constructor and deleting `Loop()`'s draw call (kept the narration message, which no longer triggers a draw itself).
- Built the hotseat handoff flow for playing on one shared screen — `HandoffTo()` ("hide your cards" / "pass the device"), three per turn: attacker → defender (privacy for the defend choice), defender → original attacker (so they see the outcome they missed while hidden), then → the new active player's fresh turn. Added `PrintDefenseSummary` (card used, damage, full status) shown at the middle handoff.
- Added `GameState.GetPendingAttackCard()`/`GetLastDefenseCard()` so the CLI can re-derive "what happened" after the screen clears; fixed `GetPendingAttackCard`'s return type, which was incorrectly non-nullable.
- Added `FormatCard` — full stat line now shown in every "attacks with"/"defends with" narration (previously just name, or name + one stat).
- Column-aligned `PrintCard`'s hand listing.
- Added player naming — prompt at match start, blank input keeps the "PlayerA"/"PlayerB" default. Purely presentation — `PlayerId` itself is untouched everywhere else.
- Authored `StarterDeck`'s first real card set — 10 Fire (`Scor`) templates, 4 copies each, 40-card deck. Only element built so far; both players still get identical decks.
- Played a full real match — confirmed it's fun. Discussed and decided: hold off on same-element-cost and on effects/leaders until playtesting specifically signals a need; hotseat play over networking for now; if networking happens later, the console harness itself can be the networked client, no GUI needed first.
- Planned the MVP roadmap toward a shareable portfolio piece: cut `Element` and same-element-cost entirely (not just deferred); locked the MVP card model to Cost/Charge/Attack/Defense only; added mulligan, a simple AI opponent, max-4-copies deck constraints, a Blazor WebAssembly front end (reusing `CardGame.Engine` unchanged), a deck builder with `localStorage` persistence + import/export, and GitHub Pages deployment to the plan. Full sequence recorded in `Architecture.md`'s Roadmap.
- Code-smell discussion: agreed not to restructure `Program.cs` given most of its console-specific code is disposable once Blazor lands. Flagged `GameState` as the real risk (permanent, about to grow with mulligan/deck-validation) and `StarterDeck` as needing a rename once it becomes a shared catalog rather than one hardcoded deck. Also identified the test suite's rigid `SetupTestContext`/`SetupGameEndContext` factories as already straining (two newer tests had to bypass them entirely) — a test-data-builder is the fix. Moved test fixtures + failure-path coverage to the top of the Roadmap.

## 2026-08-18

- Replaced `GameStateTestContext`/`SetupTestContext`/`SetupGameEndContext` with explicit per-test setup: a shared `TestCards.Card(...)` builder (optional named params, sensible defaults) and a `GameStateTests.CreateGameState(...)` helper (optional per-player decks + starting player). `GameStateTestContext.cs` deleted.
- Filled the failure-path gap: `DeclareDefense` now has rejection tests (wrong phase, active player defending, unaffordable card); `DeclareAttack` gained wrong-phase and unaffordable-card cases alongside the existing wrong-player one.
- Removed `Element` from the engine — `BattleCard` is down to `Name`/`Cost`/`Charge`/`Attack`/`Defense`, `Element.cs` deleted, all call sites (`StarterDeck`, `TestCards`, `CombatResolverTests`) updated. MVP card model is now fully in place as scoped.
- Roadmap items 1 (test fixtures) and 2 (remove `Element`) complete; remaining roadmap renumbered.
- Decided match setup (opening hands + mulligan) should be its own `MatchSetup` type rather than folded into `GameState` — expected to grow, and keeps `GameState` scoped to turn resolution only. `GameState` itself doesn't change; `MatchSetup` builds both `PlayerState`s, deals the opening hands, mediates mulligan, then hands off a ready `GameState`. Next: TDD `MatchSetup` starting with tests, migrate the existing setup logic/tests into it, then add mulligan with its own tests.
- Built `MatchSetup.StartGame` and `PlayerState.ReturnCard`/`ReturnCards` (undo a draw — the inverse of `DiscardCard`, card(s) back to the top of the draw pile). Caught a real sequencing bug while wiring up mulligan: `GameState`'s constructor already draws a "turn 1" card for the starting player, so if mulligan happens after `GameState` exists, that bonus card is indistinguishable from the opening hand and gets mulliganed too. Fixed by moving `Mulligan()` onto `PlayerState` itself (return hand, reshuffle, redraw 5) and changing `MatchSetup.StartGame` to take already-dealt, already-mulligan-resolved `PlayerState`s rather than raw decks — dealing is now the caller's job, `StartGame` is purely the pregame-to-`GameState` hand-off. `PlayerState` also now keeps the `Random` it was constructed with (was previously used once and discarded) so `Mulligan` can reshuffle deterministically in tests.
- Updated the CLI to run the full mulligan flow at match start: handoff to Player A, show hand, y/n prompt, show new hand if mulliganed, handoff to Player B, repeat, handoff back to Player A before the match loop begins.
- Discussed whether `MatchSetup` still earns its keep now that it's a one-line wrapper around `new GameState(...)` — kept it, since a planned coin-flip-for-starting-player feature is a natural second responsibility for it to own.
- Roadmap item "Mulligan" complete; remaining roadmap renumbered.

## 2026-08-19

- Implemented Multiple Attacks: `DeclareDefense` no longer calls `ReadyNewTurn` on non-lethal resolution — it just resets `Phase` back to `ReadyToAttack`, leaving `ActivePlayer` on the same attacker. `ReadyNewTurn` (replenish both, draw, swap) now fires only through `DeclareAttack`'s existing decline-to-attack path, which already had this behavior — no new intent or method needed to end a turn.
- TDD'd `DeclareAttack_PlayerCanAttackMultipleTimes_ReturnsTrue` through several real bugs before it was solid: identical test cards masking attacker/defender mixups (fixed with uniquely-named cards), attacker ids that didn't match their own comments, a missing explicit pass, and a genuine energy-affordability edge case — a defender who spends energy blocking one attack can't afford to block a second one in the same turn, since energy only replenishes on a pass. Resolved by having the defender decline rather than special-casing card costs.
- `ReadyNewTurn`'s `Phase = TurnPhase.ReadyToAttack` line was briefly deleted as cleanup, then restored as real code rather than left implicit — makes the method fully self-contained (sets up everything a new turn needs) instead of depending on its one caller already having the phase right.
- Restructured the CLI's `Loop()`: turn header/status/draw-narration now print once per real turn; an inner loop lets the same attacker keep attacking (reprinting their current hand each time) until they explicitly pass, only then running the final handoff to the next player.
- Found and fixed a bug from that restructuring: a match ending mid-turn (a later attack in a multi-attack turn lands the killing blow) left `Loop()`'s inner loop stuck asking for another attack forever — `DeclareAttack` rejects everything once `Phase` is `MatchEnded`, including a pass, so there was no valid input that could break the loop. Fixed by checking `Phase == TurnPhase.MatchEnded` right after each resolved exchange and returning immediately instead of looping back.
- Roadmap item "Multiple Attacks" complete; remaining roadmap renumbered.

## 2026-08-21

- Built `CardGame.Ai` (new project, references `CardGame.Engine` only) and its `Logic` static class: `DeclareAttack` picks the highest-`Attack` affordable card, tie-broken by lowest `Cost`, then highest `Charge`, then lowest `Defense`, then first-found; `DeclareDefense` picks the card giving the greatest damage reduction (`Defense` capped at the incoming attack, so overkill doesn't beat an exact block), tie-broken by lowest `Cost`. Both return `null` (pass/decline) when nothing affordable. TDD'd via a new `CardGame.Ai.Tests`/`LogicTests.cs`, referencing `CardGame.Engine.Tests` directly to reuse `TestCards`.
- Wired the AI into `CardGame.Cli`: a "Computer vs. Human" prompt at match start sets `s_isAi`; `IsAisTurn` gates `ProcessAttack`/`ProcessDefense` between reading console input and calling into `Logic`; a new `MaybeHandoffTo` skips the entire hotseat "pass the device" dance in AI matches (there's never a second human to hand off to); the active/defending hand is only printed when it's not the AI's, so its hand stays hidden same as any player's would be.
- Found and fixed real bugs while wiring this up: the AI defense branch was setting a local variable but never actually calling `GameState.DeclareDefense`, leaving `Phase` stuck on `ReadyToDefend` forever once the AI defended — every subsequent attack failed validation, which was the "stuck on defense" symptom. A local `string s_playerBName` inside `Main` shadowed the class field of the same name, so Player B's name/AI flag silently never reached the rest of the class. A stray extra `Console.ReadLine()` on the mode-select menu blocked on an unwanted second Enter press, and its prompt lines used `Console.Write` instead of `Console.WriteLine`.
- Found and fixed a match-end narration bug: the engine already ends the match at the correct instant (`GameState.DeclareDefense` sets `Phase = MatchEnded` synchronously the moment health hits 0) but `ProcessDefense` printed a "Next turn" banner unconditionally, and `Loop` then called `PrintDefenseSummary`, which independently recomputed and reprinted the same "doesn't defend"/damage narration a second time before checking for match end — a fatal hit looked like the game kept going. Trimmed `PrintDefenseSummary` down to just the post-defense status refresh (the one thing it was showing that `ProcessDefense`'s own narration hadn't already covered).
- Decided to deprioritize deck constraints: nothing in the app can currently produce an illegal deck (`StarterDeck.Create()` is always a fixed, legal 40 cards), so a validator has nothing to guard yet — deferred until the deck builder exists to need it.
- Roadmap item "Simple AI opponent" complete; "Deck constraints + catalog" resequenced to sit just before the deck builder UI; remaining roadmap renumbered.
- Kicked off the Blazor front end (Roadmap item 1). Settled the architecture before scaffolding anything: `GameSessionService` as a DI-registered state/orchestration service (holds `PlayerState`/`GameState`, thin wrappers around `GameState.DeclareAttack`/`DeclareDefense`, raises a `Changed` event components subscribe to for re-rendering); single page with a full-screen privacy-overlay component (driven by a new `PendingHandoffTo` field) replacing the CLI's blocking `HandoffTo` calls for the hotseat pass-the-device flow; `CardGame.Ai` wiring deferred until human-vs-human hotseat works first, same order the CLI itself was built in.
- Discussed TDD's fit for this layer: keep test-driving `GameSessionService` (pure state/orchestration logic, same shape as `GameState`/`Logic`) but not the Razor markup itself, which has no natural pass/fail assertion and is better iterated visually. Noted `bUnit` as the closest thing to component-level TDD in Blazor, but out of scope for now.
- Scaffolded `CardGame.Web` (`dotnet new blazorwasm`, references `CardGame.Engine` only) and `CardGame.Web.Tests` (xUnit, references `CardGame.Web` + `CardGame.Engine.Tests` for `TestCards`). `GameSessionService` and its test file exist as empty stubs — implementation starts next session, test-first.

## 2026-08-23

- Built out `GameSessionService` via TDD: `SetupPlayers` (creates both players, deals opening hands), `BeginGame` (calls `MatchSetup.StartGame`, throws `InvalidOperationException` if called before `SetupPlayers`), `DeclareAttack`/`DeclareDefense` (thin wrappers around the engine, returning the intent's `Success`). A first-pass `Mulligan` method was cut partway through — pure forward to `PlayerState.Mulligan()` with nothing service-specific to test yet — flagged to revisit once `Changed` exists to raise.
- Added `PendingHandOffTo` (`PlayerId?`, `null` by default) to replace the CLI's blocking `HandoffTo`. Took several rounds of red tests to land: initially non-nullable (couldn't represent "no handoff pending"); `DeclareAttack` unconditionally set it to `Game.ActivePlayer` regardless of success, and regardless of decline-vs-real-attack — a real attack should hand off to the *defender*, not stay on the attacker, since `ActivePlayer` (whose turn it is) and "who needs to act next" are different questions that only diverge mid-exchange; `DeclareDefense` never set it back to the attacker on a normal resolution, and later set it unconditionally regardless of success, wrongly moving the handoff even when the defender's card choice was rejected.
- Found and fixed real engine-wiring bugs along the way: `BeginGame` originally called `new GameState(...)` directly instead of `MatchSetup.StartGame(...)`, bypassing the engine's dedicated pregame entry point (relevant once the planned coin-flip-starting-player feature lands there). `DeclareDefense` was initially copy-pasted from `DeclareAttack` and called the wrong engine method entirely; once fixed, it was also constructing `DeclareDefenseIntent` with the *attacking* player's id instead of the defender's (via `GameState.GetOpponentId`).
- Added deterministic seeded-deck support to `SetupPlayers` (`List<BattleCard>? seededDeckA/B = null`, falling back to `StarterDeck.Create()`) — first attempt used nullable-but-required parameters with no `= null` defaults, breaking every existing call site. Used to replace random-hand-dependent test setup (picking `Hand[0]` from a real shuffle, which isn't reliably affordable) with guaranteed-affordable seeded decks, mirroring the engine test suite's existing custom-deck-injection pattern.
- Created `CardGame.DeckManagement` (new project, references `CardGame.Engine` only) and moved `StarterDeck` into it from `CardGame.Cli`. `CardGame.Web` had accumulated a byte-for-byte duplicate of the file rather than sharing it, since it had no way to reference `CardGame.Cli` — both now reference the new project instead.
- Bumped `.editorconfig`'s `csharp_prefer_braces` from `silent` to `warning`, making brace omission a build warning instead of an unenforced preference; this also surfaced a pre-existing braceless `if` in `PlayerState.DrawCards` that had been silently allowed the whole time.
- `CardGame.Web.Tests` at 10 passing tests; full solution at 65 (1 intentionally skipped).
- Remaining on `GameSessionService`: a method to clear `PendingHandOffTo`, the `Changed` event, and revisiting `Mulligan`.

## 2026-08-27

- Built `ConfirmReady()` — clears `PendingHandOffTo` back to `null`. First pass was `private` and lowercase-named (`clearHandOff`), which would've been unreachable from both tests and a future Razor click handler; fixed to `public`/PascalCase.
- Added `event Action? Changed;`, replacing an earlier attempt that declared it as a plain `Action?` auto-property — that version failed with `CS0272` the moment a test tried `+=`, since the property's setter was `private`. The `event` keyword gives outside code `+=`/`-=` access without ever allowing direct assignment or invocation from outside the class, unlike a property.
- Wired `Changed?.Invoke()` into every mutating method, catching several real bugs along the way: `IsGameOver` (a pure health-check query) had picked up an accidental `Changed?.Invoke()` inside it, causing `DeclareDefense` to double-fire on every success; `ConfirmReady` raised `Changed` *before* clearing `PendingHandOffTo`, so a subscriber reacting to the event would see stale state; `DeclareAttack`/`DeclareDefense` raised `Changed` unconditionally instead of only within their success branch; an edit to `SetupPlayers` accidentally dropped its `Changed?.Invoke()` entirely, caught by its own test going red.
- Added `changedRaised` assertions across all of `GameSessionServiceTests`, fixing one along the way: `DeclareAttack_ActivePlayerChanges_ReturnsTrue` subscribed to `Changed` before `BeginGame` ran, so it couldn't actually prove `DeclareAttack` raised the event — `BeginGame`'s own firing already satisfied the assertion regardless. Moved the subscription to after `BeginGame`, same fix already applied to `BeginGame_CreatesGameAndSetsActivePlayer` earlier.
- `GameSessionService` now has `SetupPlayers`/`BeginGame`/`DeclareAttack`/`DeclareDefense`/`ConfirmReady` all implemented, tested, and wired to `Changed`/`PendingHandOffTo`. `CardGame.Web.Tests` at 11 passing tests.
- Designed (not yet implemented) bringing `Mulligan` back: `Mulligan(PlayerId id)` instead of taking a raw `PlayerState`, matching the rest of the class's API shape; guards against being called before `SetupPlayers` or after `BeginGame`. "No second mulligan" enforcement decided to live on `PlayerState` itself (new `HasMulliganed` flag, folded into `Mulligan()`'s existing bool return) rather than the service, so the guarantee travels with `PlayerState` for any caller. On success, sets `PendingHandOffTo = GameState.GetOpponentId(id)`; `SetupPlayers` will also need to start setting `PendingHandOffTo = PlayerId.PlayerA`, since nothing currently tracks the handoff during the pregame mulligan phase. Implementation + tests start next session.

## 2026-08-28

- Implemented `Mulligan` end to end, matching the prior session's design exactly. `PlayerState` gained a `HasMulliganed` flag; `Mulligan()` now returns `false` immediately (no-op) on a second call instead of letting it happen again. `GameSessionService.Mulligan(PlayerId id)` throws if players aren't set up yet or if `Game` already exists, resolves the player, and — only on success — sets `PendingHandOffTo = GameState.GetOpponentId(id)` and raises `Changed`. `SetupPlayers` now sets `PendingHandOffTo = PlayerId.PlayerA` at the end, closing the previous gap where nothing tracked the pregame handoff.
- First pass at the service method computed its `success` bool but never returned or acted on it (`void` return type) — fixed to `bool`, gating both `PendingHandOffTo` and `Changed` inside `if (success)`, same shape as `DeclareAttack`/`DeclareDefense`.
- `Mulligan_PlayersCanMulligan`'s first draft subscribed to `Changed` before calling `SetupPlayers`, which would've let `SetupPlayers`'s own firing satisfy the assertion regardless of whether `Mulligan` fired it — same test-isolation issue caught earlier with `DeclareAttack_ActivePlayerChanges_ReturnsTrue`. Fixed by moving the subscription to right before the `Mulligan` call.
- Added `Mulligan_ThrowsIfPlayersAreNotCreated` and `Mulligan_ThrowsIfGameHasStarted`, covering both guard clauses at the service level. Skipped a service-level "second mulligan returns false" test — already proven at the `PlayerState` level (`Mulligan_ReturnsFalseIfPlayerHasAlreadyMulliganed`) and the service is a thin pass-through here.
- `GameSessionService` is now feature-complete for its original scope: `SetupPlayers`/`Mulligan`/`BeginGame`/`DeclareAttack`/`DeclareDefense`/`ConfirmReady`, all tested. `CardGame.Web.Tests` at 14 passing tests.
- Fixed VS Code Razor format-on-save: `.vscode/settings.json`'s `editor.formatOnSave` was already global, but `.razor` files had no `editor.defaultFormatter`, so there was nothing for it to invoke. Added `"[aspnetcorerazor]": { "editor.defaultFormatter": "ms-dotnettools.csharp" }`, matching a known fix pattern from a public C# Dev Kit issue (microsoft/vscode-dotnettools#218).
- Started the first Razor components, bottom-up: `Card.razor` (renders one `BattleCard`'s stats; `GameCard`/`IsPlayable`/`OnClick` parameters), then `Hand.razor` (loops a card list through `Card`, forwarding click + per-card playability), then `PlayerStats.razor` (health/energy display), then `DrawPile`/`DiscardPile` (card-back counts) — later consolidated into one reusable `Pile.razor` (`Name`/`NumCards`) once both turned out identical in shape — then `PlayerBoard.razor`, composing `PlayerStats`+`Hand`+two `Pile`s behind one `Stats`/`Hand` pair, meant to be instantiated once per player.
- Hit the same naming collision repeatedly while naming component parameters: a `.razor` file compiles to a class named after the file, so a parameter can't share that name (`CS0542`) — `Card.razor` couldn't have a `Card` parameter (renamed to `GameCard`), `PlayerStats.razor` couldn't have a `PlayerStats` parameter (renamed to `Stats`).
- Found `dotnet watch` fails on macOS with SDK 10.0.300+ due to a real upstream bug (dotnet/sdk#54309): `IntermediateOutputPath` evaluates with mixed path separators, so `dotnet watch` can't find `staticwebassets.development.json`. Fixed upstream but only targeted for .NET 11 preview 7, not backported. Worked around it by forcing `IntermediateOutputPath` to forward slashes in `CardGame.Web.csproj` — but this produced a nonstandard nested path (`obj/Debug/net10.0/net10.0/...`) that appeared to break the C# Dev Kit/Razor language server's live diagnostics (garbled type errors, failed code actions). Removed the workaround; `dotnet build` + manual browser refresh preferred over `dotnet watch` until the SDK fix ships.
- Landed on Razor conventions going forward, applied across all five components: every parameter that has no sensible default gets both `[EditorRequired]` (Razor-level call-site warning if omitted) and C#'s `required` modifier (actually satisfies nullable analysis; `EditorRequired` alone does not); no `Engine.`-qualifying types when `@using CardGame.Engine` is already in scope; `@` prefix on every parameter-binding attribute value, even a bare variable (`GameCard="@card"`), but not on plain string literals (`Name="Draw Pile"`); `<h3>` for a component's own section title, `<p>`/`<span>` for data — never `<h2>`+ for arbitrary stat text (semantically a heading, not a value).
- `Card.razor`'s `IsPlayable` isn't wired to anything visual yet (no styling difference), and `PlayerBoard.razor`'s `IsCardPlayable` still hardcodes `true` for every card — both flagged as intentional placeholders until real per-card affordability (cost vs. current energy) has somewhere to be computed from.

## 2026-08-30

- Deleted `Counter.razor`/`Weather.razor` (unremoved scaffold template pages). Built `Setup.razor` (player-name entry via `<form>`/`<fieldset>`/`<label>`, no `@page` directive) and a `Phase` enum (`Title`/`Setup`/`Play` at first) driving an `@switch` in `Home.razor`, replacing routing entirely — matches the existing "single page, no separate routes" decision.
- Added `Mulligan.razor` (a player's hand + yes/no, `EventCallback<bool>`). Reshaped `GameSessionService.Mulligan` from `Mulligan(PlayerId id)` to `Mulligan(PlayerId id, bool willMulligan)` — the old form had no way to represent "declined," so a "No" click did nothing and silently stalled the hand-off; declining now always succeeds and advances `PendingHandOffTo`, same as a successful accept. Updated the three existing `Mulligan` call sites in `GameSessionServiceTests`.
- Registered `GameSessionService` in DI (`AddScoped`, `Program.cs`) and injected it into `Home.razor`, subscribing to `Changed` (`OnInitialized`) and unsubscribing (`Dispose`) so the page re-renders on service state changes.
- Refactored `Home.razor`'s `@code` into a `Home.razor.cs` partial-class code-behind for readability. Extracted `PendingHandoffTo.razor` and `PostMulliganHand.razor` out of inline markup into their own components — first attempt at `PostMulliganHand.razor` tried to read `Home`'s injected `Game`/`_activePlayerId` directly, which doesn't work; a child component only gets data through its own `[Parameter]`s.
- Moving code out of the Razor-generated `@code` block and into a real `.cs` file surfaced four analyzer warnings that generated code doesn't get flagged with: `CA1816` (`Dispose` missing `GC.SuppressFinalize`), `IDE0044` (a field that's only ever assigned once should be `readonly`), `IDE0011` (brace-less `if`), `CA1822` (a method not touching instance state could be `static` — left as-is since it's a placeholder that'll need instance state once real logic lands). First three fixed.
- Implemented the `Play` phase: `OnCardClicked` routes to `Game.DeclareAttack`/`Game.DeclareDefense` by comparing `GameState.ActivePlayer` to a new `_activePlayerId` field (tracks whichever player currently owns the interactive screen, set only in the hand-off "Ready" handler). `PlayerBoard.IsCardPlayable` finally does a real check (`card.Cost <= Stats.CurrentEnergy`) instead of a hardcoded `true`. `Hand.razor` gained a "Pass" button invoking the same click callback with a `null` card — reuses the engine's existing null-card-means-decline semantics for both attacking and defending, no new engine surface needed.
- Found and fixed a UI freeze: an early draft of the attack branch retried `DeclareAttack` in a `while (!attacked)` loop on failure. Since a genuinely unplayable card fails identically every time, this spun forever and hung the single-threaded WASM page. Replaced with a single attempt plus a logged failure.
- Found and fixed a hand-off mismatch bug: `_activePlayerId` was also being reassigned inside `OnCardClicked` right after declaring an attack/defense, giving it two conflicting meanings ("who's viewing now" vs. "who's receiving next") depending on when it was read. The hand-off screen now reads the greeted player's name straight from `GameSessionService.PendingHandOffTo` (the authoritative value) instead of deriving it from `_activePlayerId`; `_activePlayerId` itself is now set only in the hand-off "Ready" handler.
- Found and fixed a real engine bug: `GameState._pendingAttackCard` was never reset to `null` after `DeclareDefense` resolved it, so `GetPendingAttackCard()` returned the previous round's card indefinitely. Now cleared right after `CombatResolver.ResolveBattle` consumes it, in `DeclareDefense`. New test: `DeclareDefense_ResolvesExchange_ClearsPendingAttackCard`.
- Added `PlayerBoardInformation.AttackCard` (`BattleCard?`, optional) to show the pending attack card on the opponent's info panel while defending. First draft typed it `required BattleCard` (non-nullable) despite being genuinely optional — compiled anyway since Razor doesn't enforce `required` at component call sites the way C# enforces it on object initializers, but was corrected to the accurate `BattleCard?` shape.
- `Setup.razor`'s `OnSubmit` now carries both entered names up as `EventCallback<(string PlayerAName, string PlayerBName)>` instead of firing parameterless; `Home` stores them and uses them everywhere a player name is displayed.
- Added `Phase.GameOver` and `VictoryScreen.razor`. Detected by checking `GameState.Phase == TurnPhase.MatchEnded` right after a successful `DeclareDefense`; the winner is derived from whichever player's `CurrentHealth` is 0, since nothing stores a `Winner` anywhere else.
- Built `.github/workflows/deploy.yml`: builds, tests, and publishes `CardGame.Web` on every push to `main`, then deploys to GitHub Pages (`actions/upload-pages-artifact`/`actions/deploy-pages`). Rewrites the `<base href>` in the *published* `index.html` only (to `/card-game-engine/`, the GitHub Pages project-site path) so local dev keeps serving from `/`; adds `.nojekyll` since GitHub Pages otherwise ignores the underscore-prefixed `_framework` folder. Requires a one-time repo setting (Settings → Pages → Source → "GitHub Actions") and a PAT with the `workflow` scope — first push attempt was rejected for lacking it, not yet resolved/confirmed live.
- Discussed Blazor UI test coverage: no bUnit/component-level tests exist or are planned for now — `GameSessionService`/`GameState` carry all the real logic and are fully unit-tested, so UI-layer tests would mostly re-verify wiring. Noted as worth revisiting only if the page's phase/hand-off orchestration keeps growing in complexity.
- Started real CSS work: adopted CSS isolation (`<Component>.razor.css`, already present from the original scaffold via `MainLayout.razor.css`/`NavMenu.razor.css`) as the primary styling mechanism, global `wwwroot/css/app.css` reserved for genuinely site-wide rules, inline `style=` for rare one-offs only. Decided to write custom CSS rather than lean on Bootstrap's default look; removed the Bootstrap `<link>` from `index.html`.
- Styled `Card.razor`: fixed an invalid `<article .card-article>` (CSS selector syntax used where an HTML attribute was needed — should be `class="card-article"`); converted the Cost/Charge and Attack/Defense stat pairs from `<p>` text to `<dl>`/`<dt>`/`<dd>` (the correct semantic element for label→value pairs); reset the outer `<button>` with `all: unset` (plus `cursor: pointer`, `display: block`) so its default browser chrome doesn't fight the actual visual styling living on the inner `.card-article`.
- Added `wwwroot/images/` and a `card-placeholder.png` — a file added to a repo-root `/Assets` folder isn't reachable at runtime; Blazor WASM only serves files physically under `wwwroot`.
- Styled `Hand.razor`: found the hand stopped spreading out after moving the "Pass" button from inside `<ul>` (as a fellow `<li>`) to a sibling below it — the parent `<section>`'s `align-items: center` (needed to center the button) also shrinks `<ul>` to hug its own content instead of stretching full width, leaving `justify-content: space-evenly` no space to distribute; fixed with `align-self: stretch` on `<ul>` specifically, which overrides the parent's `align-items` for just that element.
- Two CSS correctness bugs found via trial and error: `dl, dt { margin: 0; }` reset `dt` but not `dd`, and `<dd>`'s default browser margin (a ~40px left indent) was still throwing off spacing — fixed by including `dd` in the reset. Separately, removing an apparently-redundant `font-size: medium` from the Pass button made its text shrink — `<button>` (and other form controls) don't inherit typography normally, browsers give them their own smaller native control font via the UA stylesheet, so `font-size: medium` wasn't a no-op, it was overriding that. Fixed with `font: inherit`, the general-purpose version of the same fix already applied to `.card-button`.
- Confirmed `dotnet watch`'s known macOS SDK 10.0.300+ bug (see 2026-08-28) is still unfixed on the currently-installed 10.0.400 and is the likely cause of watch not picking up `.razor.css` changes specifically (it can't find `staticwebassets.development.json`, which scoped CSS bundling depends on). Recommended routing around it with an external file-watcher (`fswatch`) invoking a plain `dotnet build` directly, since that sidesteps `dotnet watch`'s own broken rebuild-detection rather than `dotnet build` itself. Re-confirmed the earlier forced-forward-slash `IntermediateOutputPath` workaround (which broke IDE diagnostics) is not worth retrying.
- Built a disposable style-preview harness in `Home.razor`/`Home.razor.cs`: swapped the `Phase.Title` case's real "Start Game" button through a static `Card`, then a static `Hand` (`_previewHand`), then a full `PlayerBoard` (`_previewHand` + a new `_previewStats` `PlayerState`, built in a new `Home()` constructor since the field is `readonly`) — each purely to have something on-screen to iterate styling against without playing through Setup/Mulligan every time. Deliberately left `PlayerBoard`'s `OnCardClicked` unbound in preview mode, since `Home`'s real handler dereferences `Game.Game!`, which is `null` before `BeginGame` runs. Plan going into the next session: branch this preview state off before restoring the real Title-phase flow, so `main` stays playable/deployable.
