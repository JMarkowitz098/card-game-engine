@using CardGame.Engine

<section>
    <h2 class="visually-hidden">Player Board</h2>
    <div class="player-info">
        <CurrentPlayerState IsAttacking="@IsAttacking" CurrentPlayer="@PlayerName"></CurrentPlayerState>
        <PlayerBoardInformation Stats="@Stats" />
    </div>
    <Hand Cards="@Hand" OnCardClicked="@OnCardClicked" IsCardPlayable="@IsCardPlayable" />
</section>

@code {
    [Parameter, EditorRequired]
    public required PlayerState Stats { get; set; }

    [Parameter, EditorRequired]
    public required IReadOnlyList<BattleCard> Hand { get; set; }

    [Parameter, EditorRequired]
    public required string PlayerName { get; set; }

    [Parameter]
    public EventCallback<BattleCard> OnCardClicked { get; set; }

    [Parameter, EditorRequired]
    public required bool IsAttacking { get; set; }

    public bool IsCardPlayable(BattleCard card)
    {
        return card.Cost <= Stats.CurrentEnergy;
    }
}