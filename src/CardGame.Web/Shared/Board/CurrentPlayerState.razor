<section>
    <h3>@CurrentPlayer is @(IsAttacking ? "Attacking" : "Defending")</h3>
</section>

@code {
    [Parameter, EditorRequired]
    public required bool IsAttacking { get; set; }

    [Parameter, EditorRequired]
    public required string CurrentPlayer { get; set; }
}