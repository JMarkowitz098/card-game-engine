@using CardGame.Engine

<div @onclick="() => OnClick.InvokeAsync(GameCard)">
    <div>
        <p>@GameCard.Name</p>
        <p>Cost: @GameCard.Cost</p>
        <p>Charge: @GameCard.Charge</p>
        <p>Attack: @GameCard.Attack</p>
        <p>Defense: @GameCard.Defense</p>
    </div>
</div>

@code {
    [Parameter, EditorRequired]
    public Engine.BattleCard GameCard { get; set; }

    [Parameter]
    public bool IsPlayable { get; set; }

    [Parameter]
    public EventCallback<Engine.BattleCard> OnClick { get; set; }
}