@using CardGame.Engine

<div>
    <h3>@Name</h3>
    <span>Cards: @NumCards</span>
</div>

@code {
    [Parameter, EditorRequired]
    public int NumCards { get; set; }

    [Parameter, EditorRequired]
    public string Name { get; set; }
}